namespace LetterGarden.Core
{
    public enum WordSubmitResult
    {
        Invalid,
        AlreadyFoundRequired,
        AlreadyFoundBonus,
        RequiredWordFound,
        BonusWordFound
    }
}
